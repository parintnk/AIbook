import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Saved — idea" };

export default function BoardsPage() {
  return (
    <ComingSoon
      title="Saved boards"
      description="Your boards — followable collections of saved workflows — land here in a later release."
    />
  );
}
