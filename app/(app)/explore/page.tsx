import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Explore — idea" };

export default function ExplorePage() {
  return (
    <ComingSoon
      title="Explore"
      description="Cross-profession discovery and the daily featured workflow land here in a later release."
    />
  );
}
