import { SettingsSectionStub } from "@/components/settings/settings-section-stub";

export const metadata = { title: "Danger zone — idea" };

export default function DangerSettingsPage() {
  return <SettingsSectionStub title="Danger zone" />;
}
