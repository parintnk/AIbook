import { SettingsSectionStub } from "@/components/settings/settings-section-stub";

export const metadata = { title: "Account — idea" };

export default function AccountSettingsPage() {
  return <SettingsSectionStub title="Account" />;
}
