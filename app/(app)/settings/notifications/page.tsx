import { SettingsSectionStub } from "@/components/settings/settings-section-stub";

export const metadata = { title: "Notifications — idea" };

export default function NotificationsSettingsPage() {
  return <SettingsSectionStub title="Notifications" />;
}
